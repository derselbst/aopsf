Todo:

- Fix all compatibility issues that are actually fixable
- Add better documentation
- Add build scripts and files


To use for now:

Compile all C source files to a library, and import psx_external.h for the
interface.